[toolchain]
channel = "nightly"
# Nightly is required because we need a basic form of reflection, features like 
#![feature(associated_type_defaults)] are locked behind nightly therefore we require
# using nightly to build. 
